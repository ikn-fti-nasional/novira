# pnpm 10 settings (package.json's `pnpm` field is no longer read).
onlyBuiltDependencies:
  - esbuild
  - sharp
  - "@node-rs/argon2"

# GHSA-pxg6-pf52-xh8x: @sveltejs/kit 2.x still depends on cookie ^0.6.0,
# which is vulnerable. 0.7.x is API-compatible and patched.
overrides:
  cookie: ^0.7.2
