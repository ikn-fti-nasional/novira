engine-strict=true
# drizzle-orm lists better-sqlite3/pg/mysql2 etc. as optional peers for its
# non-Postgres drivers; we only use postgres.js + pglite, so skip auto-installing
# the native SQLite binding (leftover from the pre-Postgres migration).
auto-install-peers=false
